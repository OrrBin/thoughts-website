export class ColorImage {
  width: number;
  height: number;
  path: string;

  constructor(width: number, height: number, path: string) {
    this.width = width;
    this.height = height;
    this.path = path;
  }
}
